{
  "name": "@livepeer/clearinghouse-ui",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": "./src/index.ts",
  "dependencies": {
    "lit": "3.3.3"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage && npm --prefix ../.. run coverage:denominators -- ui"
  },
  "customElements": ".ci-cem-inspect/custom-elements.json"
}
