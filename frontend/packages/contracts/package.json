{
  "name": "@livepeer/clearinghouse-contracts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": "./src/index.ts",
  "dependencies": {
    "effect": "3.22.2"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage && npm --prefix ../.. run coverage:denominators -- contracts"
  }
}
